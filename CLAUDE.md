# Instructions for Claude

**NEVER build this project.** Do not run `gradle`, `./gradlew`, `bash build.sh`, `npm run build`, or any build command. There is no `build.sh` — do not recreate it.

GitHub Actions (`.github/workflows/build.yml`) handles all builds. Your job is to edit sources only.

After editing, stop. Do not attempt to verify compilation locally.
